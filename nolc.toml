[projet]
nom = "nol-serde"
version = "0.3.0"
entree = "src/lib.nol"
auteur = "daliranas <bastien@languedoc.ovh>"
licence = "MIT"
description = "JSON fiable en pur Nolc : encodeur (échappement correct) et décodeur (parseur, validation, extraction typée)."

[dependances]
